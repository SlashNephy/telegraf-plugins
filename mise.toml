[tools]
golangci-lint = "2.13.0" # go より先に配置しないと go/bin/ のバイナリが優先されてしまう
go = "1.26.6"
goreleaser = "2.17.1"

[tasks.build]
run = "go build ./..."

[tasks.test]
run = "go test ./..."

[tasks.lint]
run = "golangci-lint run --fix"

[tasks.release-check]
run = "goreleaser check"
